# 上传服务产生的数据：每个人自己的曲库和板块
# （.gitignore 只是建议——你要是想让这些跟着仓库走，把下面几行删掉就行）
data/*
!data/.gitkeep
media/*
!media/.gitkeep

# 自检脚本的截图
.check/

node_modules/
*.tmp

# 云村小服务的登录凭证（相当于你的网易云登录态，绝对不要提交）
.ncm-session.json

# 本机部署实况：域名、隧道 ID、本机绝对路径这些「只对一台机器成立」的值。
# 公开仓库里只留 deploy/local.config.example.ps1 这份模板。
# 模板：Copy-Item deploy\local.config.example.ps1 deploy\local.config.ps1
deploy/local.config.ps1
deploy/LOCAL-DEPLOY.md

# Cloudflare Tunnel（长期部署用，见 deploy/CLOUDFLARE-TUNNEL.md）
# 后台源站的日志开头就印着上传口令，不能进仓库
deploy/*.log
# cloudflare-tunnel-skill 助手的运行时状态（里面可能有 tunnel 地址与 pid）
.cloudflare-tunnel/
# DSH web 的带 token 入口文件（token 就是远控进程的钥匙，永远不进仓库）
deploy/dsh-web-url.txt
# cloudflared 的凭据与 token：只放在 %USERPROFILE%\.cloudflared\，永远不要进来
*.cfargotunnel.json
cert.pem
