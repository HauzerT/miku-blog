# 更新日志

本站的版本线。次位加一是有新功能进场，末位加一是修修补补。
（1.1 及之前的历史按提交归纳，细节见对应的提交与 README。）

## 2.1.3 - 2026-09-18

### 文档

- **仓库首页瘦身，另外两份说明各归其位**：README 从 1480 行收到 120 行，只留
  「这是什么」与视觉哲学《校准之息》——原来这两块被文件地图、上传系统与部署流程
  埋在了中间，现在点开仓库第一眼就是它们。搬走的部分按用途分成两份：**怎么跑、
  怎么用、怎么发**进 [HANDBOOK.md](HANDBOOK.md)（操作手册），**设计是怎么来的**
  （设计计划、设计系统速查、窄屏读法、已知取舍、设计资产）进 [DESIGN.md](DESIGN.md)
  （设计志）。README 顶上一张表把这几份文件连起来，`design/` 下那两份指路牌、
  `assets/vendor/README.md` 与 `deploy/CLOUDFLARE-TUNNEL.md` 里指向旧位置的引用
  也一并更新。

## 2.1.2 - 2026-09-18

### 修复

- **公网入口重新可达**：`deploy/start-blog-background.ps1` 被存成了不带 BOM 的
  UTF-8，Windows PowerShell 读不对里面的中文，脚本在加载阶段就报错、一行都执行
  不到。它又是被看门进程调起来的，报错被吞掉，任务计划程序那边看着「首跳都跑过
  了」，源站却始终没起来，公网入口一直回 502。补回 BOM 之后源站正常拉起，看门
  循环也终于能真正兜住它了。

## 2.1.1 - 2026-09-18

### 文档

- **版本管理规矩收紧**：版本号从此严格按规则自动算好落笔（新功能进次位、修补进
  末位、破坏性变更进主位），不再每次征求站长意见；发布提交与 `v` 标签照常打在
  本地，但**默认不推送远端**——推不推、何时推，由站长自己动手。

## 2.1.0 - 2026-09-17

### 新功能

- **启动只剩一条命令，而且先把入口摆在眼前**：双击 `start.cmd` 会同时拉起 Nuxt
  应用与云村小服务，然后打一张横幅——**入口地址**（`http://127.0.0.1:4321/`）、
  **门厅开着还是关着**、**口令**、云村页与小服务的地址。以前这些是散着说的，
  「打开哪一页都会先撞上门厅」只写在 README 里；现在横幅直接告诉你，而且那一行
  是照着 `data/settings.json` 说的实话：写着 `"gate": false` 时会当场把话说明白，
  并指出去哪一行把它去掉。
- **缺构建就自己构建**：第一次跑、或者换了台机器，不再需要你记住
  `pnpm install` / `pnpm build` 两条命令——没有 `.output/server/index.mjs` 时
  `start.ps1` 会自己装依赖再构建（构建失败才报错退出）。部署与开机自启那种场合
  不该触发一分钟的构建，所以它们传 `-NoBuild`：那时缺产物立刻失败，并把两条命令
  写清楚。
- 新开关 `-Open`：起好之后顺手用默认浏览器打开入口（会先落在门厅）。连同原有的
  端口参数与 `-NoKumura`，日常就是 `start.cmd`、`start.cmd 3000`、
  `start.cmd -NoKumura`、`start.cmd -Open` 这几种。

### 变更

- 默认端口仍是 **4321**：隧道 origin 指着它，换端口要两边一起改（横幅里的地址会
  跟着变）。
- README 的「启动」一节改成先讲入口横幅与那三个开关，工具表同步；`start.cmd` 的
  注释跟着更新（它仍然保持纯 ASCII，中文只写在 `.ps1` 里）。

## 2.0.0 - 2026-09-17

### 破坏性变更

- **整站换成 Nuxt 3（Vue 3 + Vite + Nitro），1.x 那条零构建静态线整条收掉**：
  页面不再是一堆生成出来的 HTML 加手写 IIFE，而是 Vue 组件；服务端由 Nitro 提供
  接口、鉴权、门厅与内容装配。生成出来的 `index.html` / `sections/` / `posts/`、
  手写的 `server/server.mjs`、给静态页用的那批 `assets/js/*.js`、`tools/build.mjs`
  与它那批自检都不在仓库里了。两条写了很久的底线——**零构建**与**双击 `index.html`
  （`file://`）就能看**——随之取消：跑这个站要 `pnpm install` 一次、`pnpm build`
  一次，再 `node .output/server/index.mjs`（或双击 `start.cmd`，它会替你检查构建
  在不在）。老地址一个不断：`index.html`、`archive.html`、`login.html`、
  `sections/<板块>.html`、`posts/<slug>.html` 一律 301 到新路由。
- **跑起来的方式变了**：Nuxt 应用是 `pnpm dev`（开发），或者 `pnpm build` 之后
  `node .output/server/index.mjs`（成品）。旧地址一个都不用改——`login.html`、
  `editor.html`、`kumura.html`、`archive.html`、`sections/*.html`、`posts/*.html`
  一律 301 到新路由，浏览器里那把口令（`localStorage` 的 `cv01-key`）接着用。
- **发到静态托管时没有门厅**：门厅是服务端在拦，静态托管没有这一层（`data/*.json`
  也进不了产物）。想在静态托管上挡人得另找一层，比如 Cloudflare Access。
  这一条对 1.x 那条零构建静态线同样成立。

### 新功能

- **页面是路由，歌不断**：站内跳转交给 Vue Router，右下角那根 `<audio>` 活在壳里、
  不在任何页面里，所以切板块、翻文章、按浏览器后退都不会从头再放一遍。这条以前
  靠 `assets/js/nav.js` 手工换 `<main>` 换来，现在是框架白送。
- **门厅搬到 `/login`，而且有了自己一张壳**：只加载配色、token、base 与门厅四张
  样式表，不再借站内那几张。两颗键的行为一字未改——访客进入会当场忘掉口令
  （补上过的那道漏），站长登录把口令交给浏览器密码库，`#owner` 直达，
  `?leave=1` 上锁，三十天的章。
- **编辑页搬到 `/editor`**：Markdown 左写右看、按钮 / 拖拽 / `Ctrl+V` 粘截图都能传，
  预览走与文章页同一个渲染器，右栏「我写的」可编辑可删。
- **云村搬到 `/kumura`**：扫码登录、账号卡、我创建的歌单架、每日推荐、红心歌单
  分页与本页播放条都搬了过来。二维码**仍由本站自己画**（复用同一份编码器，
  不引任何第三方图片服务），小服务没在跑时照旧明说是哪条命令。
- **音乐盒与站长工具箱**：右下角两颗球。音乐盒公开——能播、能选、能设默认，
  浏览器不许没交互就出声时点亮粉灯、等第一个手势续播；站长工具箱要口令——
  新建板块 / 子板块、上传音乐、快速写博客，音乐盒里的换 / 改名 / 删也只对有
  口令的浏览器显示。令牌失效时是「把你请进来验一次再重放」，不是撞墙。
- **全局编辑与右键菜单一起搬了过来**：命令栏那颗「全局编辑」一按，虚线框里的
  文字点一下就直接改，光标落在你点的那个字上；空的简介 / 导语能就地「＋ 新建」。
  右键仍是改名 / 撤下 / 恢复，两层去向没变——界面写的真删，`content/posts.mjs`
  里的原生文章只进 `data/overrides.json` 覆盖层，源文件一个字节不动。
- **钢琴声**（默认关）：点轨道名先响一声再切模块，掠过音符试听；真采样缺哪个音
  就用 WebAudio 现场合成顶替。

### 修复

- **Nitro 默认监听所有网卡**（实测 `Listening on http://[::]:4321`），而门厅「只听
  本机」的假设、试错限速按来客计数的假设、隧道文档里「只有 cloudflared 连得上这台
  服务」的前提，全建立在「只听 127.0.0.1」之上。`start.ps1` 现在写死
  `NITRO_HOST=127.0.0.1`（实测只剩 `127.0.0.1` 在听），文档同步写明别改成 `0.0.0.0`。
- **`PATCH /api/music/settings` 以前永远 404**：路由里 `/api/music/:id` 抢在
  `/api/music/settings` 前面，把 `settings` 当成了一首曲子的 id。这个 bug 从 1.x
  就带着，换成文件路由之后静态段优先，它真的能用了。
- **`/about` 里那些「没有框架、没有构建」的说法按现状重写**，门厅 / 编辑页 /
  云村的地址也一并改成新路由。
- 顺手修掉一个水合报警：关于页那几条参数末尾留着旧页面的闭合标签，注进去会
  凭空多出一个空段落。

### 变更

- **内容仍是两个源，但装配只剩一个地方**：`content/posts.mjs`（手写的一半）加
  `data/*.json`（界面写的一半）由服务端合成一份，正文在服务端就排好版。旧站为
  「建完立刻看得见」写了四十多 KB 的就地改 DOM 脚本；现在内容本来就是一份共享
  状态，改完重取一次就够。
- **卷帘的几何只剩一份**：节奏槽与时间轴接龙的算式搬进 `content/roll.mjs`，
  生成器、服务端与浏览器共用，不再各写各的。
- **样式表与 class 名一个字节没改**：`assets/css/` 原样使用，Vue 组件只负责吐出
  同一批 class。所以 2.0 的页面与 1.x 的页面看起来是同一张页面——迁移时用
  `tools/nuxt-parity.mjs` 逐节点对照过外壳骨架与每颗音符的（音高，宽度），五类页面
  全部一致（那个工具随静态页一起删掉了：没有对照物了）。
- **首页 hero 那一句挪进内容源**（`content/posts.mjs` 的 `site.hero`），生成器与
  Nuxt 读同一份，不会再被下一次 `node tools/build.mjs` 冲掉。
- 旧的 `server/server.mjs` 与那套零构建静态页**整条收掉**（同一个功能两份实现，
  改一处要改两遍，放着就会漂）。留下来的都与运行时有关，一个没动：正文渲染管线
  （`server/lib/` 的 markdown / emoji / htmltext）与试错限速、`assets/js/palette.js`
  以及云村在用的 `qr.js` / `music.config.js`、`tools/tokens.mjs` 与云村小服务——
  二维码编码器因此仍然只有一份，没有第二份拷贝。
- **两个起停脚本改成驱动 Nuxt**：`start.cmd` / `start.ps1` 起
  `.output/server/index.mjs`（构建不在就明说「先 `pnpm install` + `pnpm build`」并以
  非零退出；横幅照旧打印上传口令），`stop.cmd` / `stop.ps1`、`tools\owner.cmd` 与
  `deploy/` 里那几个后台脚本一并跟上。

### 测试

- 新增 `tools/nuxt-check.mjs`：零依赖的浏览器自检（Node 自带 WebSocket 直接跟
  无头 Chrome 说话，不装 puppeteer），一页一页看控制台、该有的元素、主题按钮、
  卷帘扫光、每日一句与门厅两颗键（21 项）。
- 新增 `tools/nuxt-studio-check.mjs`（42 项：音乐盒、浏览器拦自动播放时的粉灯
  路线、换页歌不断、站长工具箱与口令框）与 `tools/nuxt-edit-check.mjs`（83 项：
  全局编辑、点字即改、右键改名 / 撤下 / 恢复、正文富文本与覆盖层）——它们取代了
  1.x 那批只认静态页的自检。
- `tools/preflight-check.mjs` 改成只针对 Nuxt 的起法；`tools/tokens.mjs` 的接线
  校验改扫 `.vue` 与 `.mjs`，并认识 Vue 行内样式里的 `--x` 写法。

## 1.7.5 - 2026-09-17

### 修复

- **公开仓库不再附带这台机器的实况**：`deploy/CLOUDFLARE-TUNNEL.md` 是公开的
  部署文档，但它原来把「本机真实跑着的东西」整节写在里面——完整隧道 ID、两个
  真实域名、Windows 用户名、DSH 的绝对安装路径、计划任务名。这些值对读代码的
  人没用，对想找入口的人很有用，而这份文档就挂在 GitHub 上。现在那一节只剩
  占位值和一条指向本地文件的说明，真实值搬进 `deploy/local.config.ps1` 与
  `deploy/LOCAL-DEPLOY.md`（两个都在 `.gitignore` 里），另留一份
  `deploy/local.config.example.ps1` 模板进仓库。
- **两个部署脚本改为读本地配置**：`start-dsh-web.ps1` 与
  `start-tunnel-background.ps1` 每次运行都重新读 `local.config.ps1`；文件不在
  就打印提示直接退出——宁可不起，也不拿错域名去起隧道。新机器从模板复制一份
  填四个值即可。
- CHANGELOG 里那条把域名写成占位表述。历史里的旧值仍在（已经公开过，不重写
  历史就删不掉），但不会再新增。

### 变更

- **提交前多了一道密钥扫描**：新增零依赖的 `node tools/secret-scan.mjs`，扫
  通用凭据（GitHub 令牌、云厂商 key、私钥、JWT、Cloudflare tunnel token、
  网易云登录 cookie、写在赋值里的口令），外加一件 gitleaks 做不了的事——拿
  本地配置当尺子，查「本机实况有没有漏回仓库」。配套 `.githooks/pre-commit`
  （提交时只扫暂存区，命中就拦）与 `.github/workflows/secret-scan.yml`
  （push / PR 跑全量，gitleaks 官方 action 与自带扫描器各跑一遍）。装了
  gitleaks 就用它，没装也能跑。钩子要每个克隆启用一次：
  `git config core.hooksPath .githooks`。
- **已经公开过的历史旧账记进 `.secret-scan-baseline.json`**（只存哈希，不存值）：
  它们照常列出来，但不再让构建变红——一个永远红的门最后一定会被 `--no-verify`
  绕过，基线就是为了让它一直是个能过的门。`--strict` 连旧账一起看，
  `--update-baseline` 认下新命中。
- README 补「部署实况不进仓库」与「提交前扫一遍」两节，AGENTS.md 补钩子启用
  方式与「别把本机值写进公开文件」的约定。

## 1.7.4 - 2026-09-17

### 修复

- **同一个月的几篇不再收进色块，音符一个不少地摊开**：月份色块是「同月音符
  挤在同一竖列」年代的权宜之计——卷帘改成接龙排布之后，同月的几篇本来就
  一篇挨一篇排在各自的轨道里，色块反而把音符盖住，要多点一下才能看见。
  现在色块整套拆掉，每枚音符常亮常点；同月的篇数挪到顶部月份牌子后面
  （如「2025.01 · 9 篇」，单篇的月份不报数），运行时写了新文章也按同一
  规则重排。

## 1.7.3 - 2026-09-17

### 文档

- **关于页的参数表按现状重写**：那张表停在门厅出现之前，跟站点现在的行为对不上。
  补上「门厅」（访客直进 / 站长口令、三十天的章、三十二位自动口令、一键进门、
  试错限速）与「改字」（全局编辑模式、站长右键菜单、原生文章改动进覆盖层）两条；
  其余各条一一对齐——建站方式承认随站自带的 marked / KaTeX 两个本地库与
  「跑起服务才有的另一半」，内容源改成手写与界面写各占一半，首页卷帘写明是
  按日期排的时间轴（同月收进色块、没发文的日子不留空白），九个原生音写明
  已换真钢琴实录采样，写文章从「Markdown 子集」改成 GFM 全量 + KaTeX 公式 +
  emoji 短代码，听歌补上云村的歌单架、每日推荐与红心按加入时间倒序。
- 页脚年份 2025 改成 2026：站点 2026 年 9 月才立起来。这一行是内容真源里的
  一个字段（`content/posts.mjs` 的 `site.footer`），改完全站 41 个生成页跟着重建。

## 1.7.2 - 2026-09-17

### 修复

- **卷帘时间轴不再给没发文的日子留空白**：横轴原来是真实日历——两篇文章
  之间隔了多久，卷帘上就空多久，隔上几个月就是一大段推不动的空白。现在
  改成接龙：文章按写作顺序一篇紧挨一篇往右接，隔三天还是十个月，都只是
  一个音符的宽度加一格休止；顶部每个月的牌子挂到那个月第一篇音符的起点，
  右上角读数仍然报真实的月份跨度。

## 1.7.1 - 2026-09-17

### 修复

- **远控重启脚本的等待窗口放宽**：DSH web 冷启动要加载插件与会话，打印带
  token 的入口链接偶尔远超 60 秒，重启脚本会提前放弃、不写
  `deploy\dsh-web-url.txt`（实例本身照常起来、浏览器照常弹开，只是文件缺位）。
  现在等满 180 秒再判超时，不再冷启动翻车。

## 1.7.0 - 2026-09-17

### 新功能

- **同一个月的文章收进一个色块**：时间轴上同一个月里跨轨道的音符原本挤在同一竖列，
  堆在一起也难点准。现在同月有几篇的就归拢成一块月份色块，块上写着年月与篇数；
  默认收着，点一下展开成各自轨道里的音符再挑，再点一下收回去（一次只开一个月）。
  只有一篇的月份不成块，那枚音符照旧亮着；不支持 JS 的浏览器里色块自动让位，
  所有音符摊开照常点。
- 站点挂上正式域名（具体域名与部署实况记在 `deploy/LOCAL-DEPLOY.md`，那份不进仓库；
  部署侧同时加了 DSH 远控入口与常驻看门狗，访客无感）。

## 1.6.1 - 2026-09-17

### 修复

- **时间轴在常见桌面宽度下也能左右滚了**：上一版画布按「一天 0.7rem」算，92 天
  约 1226px，在 1440–1920 的屏幕上被拉伸铺满整行，没有溢出也就滚不动。现在桌面
  一天 1.15rem、手机一天 0.8rem，并保证画布至少有视口的 1.2 倍——任何屏幕上
  时间轴都留得出一截可翻的余地；文章变多之后，画布仍按日子自然变长。

## 1.6.0 - 2026-09-17

### 新功能

- **云村页新增「我创建的歌单」货架**：扫码登录后，账号卡下面就是歌单架——每个
  歌单一格正方形封面，封面直接用网易云回传的那张（经本机小服务代理取图），页面
  上和网易云永远同一张脸；格子下面写歌单名与曲目数，点整张卡落到网易云的歌单页。
  红心歌单有自己的整段展示，不在货架里重复出现。
- **云村页新增「每日推荐」**：网易云按听歌口味每天生成的那 30 来首，就列在歌单架
  下面，注明「每天 6:00 更新」；和红心歌单共用同一套曲目行——点歌、播放条、
  上一首 / 下一首、进度与音量都是现成的那套。
- **红心歌单按加入时间排序**：以前列表顺序是「接口给什么就什么」，现在按每首歌
  收进歌单的那一刻（网易云自己记的时间戳）倒序——最新红心的排在最前，和手机
  App 里的默认顺序一致；每一行右侧标出加入日期，歌单头写明「按加入时间 · 最新
  在前」。万一时间戳取不到，就回退旧顺序、不标日期也不标这句，不说没把握的话。

## 1.5.0 - 2026-09-17

### 新功能

- **首页的卷帘目录变成一条真正的时间轴**：横轴从手工排的小节换成真实的日历——音符
  站在文章写下的那一天，越往右越新，越宽读得越久。月份刻度（2025.01–03）与贯穿
  轨道场的月线按文章日期摆出；顶栏读数从「小节号 / BPM」换成篇数与时间跨度；
  悬停在音符上，读数会报出它的日期。
- **左右滚动是真的在翻日历**：时间轴的画布按日子算宽——文章越写越多，轴就往右长。
  手机和窄窗口里左右滑动，就是从一月滑到三月的过程；最后那段留白是还没写的日子，
  播放头扫过最后一格音符就停在那里。
- 运行时新写的文章按自己的日期落到轴上：浏览器按与生成器同一把比例尺，把音符、
  刻度、月线和画布宽一起重排，不用重新生成页面。

## 1.4.1 - 2026-09-17

### 文档

- **README 有了真正的目录**：开头新增按阅读动线分组、可以点跳的文档目录；原来那张
  62 行的文件大清单改成按角色分组的「文件地图」（页面 / 内容与配色 / 上传服务 /
  工具与自检 / 前端 / 设计与部署），补上此前漏列的换口令、限速自检等脚本，
  卷首还放上了首页预览图。
- **设计文档搬进了 README**：design/ 里的《设计计划》与《校准之息》（视觉哲学）全文并入，
  分别成为「设计计划」与「设计哲学 · 校准之息」两节；原来两份文件改成指回 README 的
  指路牌，文末新增「设计资产」一节，观测图海报与首页预览直接可见。
- 顺手修了两处旧账：删掉「观测图海报仍是旧配色」的过时说明（海报已随现行配色重做导出）；
  门厅配色一段误挂的「站长右键」标题改回「门厅的配色」。

## 1.4.0 - 2026-09-17

### 新功能

- **原生九个板块换上真钢琴**：开启音效后，键盘九个音（A3–A5，含黑键 F#3）响的
  是真实立式钢琴的实录采样，不再是合成的电钢音色。九个 mp3 按音高命名放在
  `assets/audio/`（合计约 1.5 MB），采样来自 VSCO 2 Community Edition
  （Versilian Studios 录制，CC0 授权，可任意使用），来源与授权说明在同目录 README。
  新建板块挑的其他音高暂时仍由合成音色顶替，想补哪个按命名规矩丢进对应 mp3 即可。

## 1.3.2 - 2026-09-17

### 修复

- **门厅第二次进门不再要口令**：点「站长登录」的那一下，页面会用 localStorage
  里记着的口令直接试门——对了一击进门（零输入），`login.html#owner` 书签同待遇；
  口令换过了（吃 401/403）就当场把旧账清掉、黑板说明原因，回到手输。
  「访客进入」的降级路径不受影响（清空 localStorage 后自动试门自然失效）。
  浏览器密码库保留为第二重保险（换浏览器、清站点数据时替你填）。

### 测试

- 门厅自检 `login-check.mjs` 扩到 38 项：新增「记着口令一击进门」「旧口令 401
  清账归零」「黑板说话」，并在头注提醒自检自己会故意错口令、别在已被限速
  打爆的服务上跑。

## 1.3.1 - 2026-09-17

### 新功能

- **门厅支持浏览器的密码管理器**：口令栏不再拦着浏览器（`autocomplete="current-password"`
  并补上 `name`），站长登录成功后还会把凭证显式交给浏览器的密码库（PasswordCredential，
  Chromium 系；Firefox 没有这套 API 就靠属性自己提示，存不上就静默算了）。
  保存一次之后，进门变成「站长登录 → 口令栏已经自己填好（或 Windows Hello 解锁）→ 进入」，
  不用剪贴板更不用背。凭证加密存放在浏览器里、随浏览器账号同步。
  访客路径不受影响：凭证只存在站长自己的浏览器里，且只有点了「站长登录」才可能被填；
  「访客进入」照旧清空 localStorage，不会带着站长权限进门。
- **`tools\owner.cmd` 一键进门**：起服务（幂等）→ 口令进剪贴板 → 用默认浏览器打开门厅
  （`#owner` 直接停在口令那一行）。一次双击顶过去的三步；401 自愈弹框同样放开了属性，
  弹出来时浏览器已经替你填好。

### 文档

- README「这么长我自己怎么登录（不用手打）」重排为三条路（owner.cmd 一键 → copy-key
  粘贴 → 浏览器密码库），并写清 cookie / localStorage / 浏览器密码库三份凭据各管什么。

## 1.3.0 - 2026-09-17

### 新功能

- **口令试错限速**：口令错头 5 次不罚，之后每错一次的等待时长翻倍（2s → 4s → 8s…），
  封顶 15 分钟；等待期内重试回 429 并带 `Retry-After`。口令输对即清空该记录。
  限速按来客计数——默认只认本机地址，挂 tunnel 时用 `CV01_TRUST_PROXY=1`
  才按真实 IP，启动横幅会把当前模式印出来。记录只在内存里，重启即清空。
- **口令可以换，而且不用手打**：`tools/set-passphrase.mjs` 生成一条 32 位随机口令
  （约 186 bit 熵）写进 `data/settings.json`；`tools/copy-key.cmd` 把它放进剪贴板，
  门厅那一栏 `Ctrl+V` 就进去了。首次启动自动生成的口令也从 8 位十六进制提到了 32 位。
- **上线前自检** `tools/preflight-check.mjs`：读配置（口令长度、门厅开关、凭据有没有
  跑进仓库）+ **真的去敲每一页**（不盖章能不能读到、服务端数据与源码是不是 404、
  写接口不带口令是不是 401），并把当前设计上公开可读的接口列出来让人确认一遍。
- 限速自检 `tools/authlimit-check.mjs`（19 项纯逻辑用假时钟跑完几小时的退避 + 5 项真接口）。
- `deploy/` 长期部署手册（Cloudflare Tunnel + Access 分步流程、开机自启、速查表）
  与幂等的后台源站启动脚本；`-PublicDeploy` = 不启动云村小服务 + 限速按真实来客。

### 修复

- **`.ncm-session.json`（网易云登录态）曾经能被公网直接 `GET` 走**：静态服务挡的是
  目录名白名单，拦不住站点根目录下的单个点文件。现在任何一段以 `.` 开头的路径都不外发，
  `deploy/`（那里的后台日志开头就印着口令）也进了名单。是上线前自检真敲出来的，不是读代码猜的。
- 口令比对从 `===` 改成定长比较，不再按第一个不同的字节短路。
- `/api/render`（不要口令是设计如此，编辑页预览靠它）加了闸：正文上限 256KB、
  同时在渲染最多 2 个，超了 429——它不落盘，但白算 CPU。

### 文档

- README 新增「这么长我自己怎么登录（不用手打）」与「挂到公网之前：先跑一次预检」；
  口令一节补上轮换与限速的来龙去脉。
- `deploy/CLOUDFLARE-TUNNEL.md` 第 9 节：门厅留着当「开屏过场」、Access 盖在前面，
  两层各回答什么问题。

## 1.2.0 - 2026-09-16

### 新功能

- **全局编辑模式**：命令栏右侧新增「全局编辑」按钮（服务在线且有口令的浏览器才看得见），
  一按整站进入编辑模式——虚线框里的文字**点一下就直接改，全程无需右键**：
  板块页的板块名 / 简介 / 导语，文章页的标题 / 正文，任何页面上的轨道栏板块名、
  首页索引（板块名与简介行）、卷帘轨道头、文章行标题。
- **空简介 / 空导语就地新建**：板块简介没写过（含动态板块页的「新建板块」占位）时，
  框里摆「＋ 新建简介」；导语整段缺失时（动态页连 `<p class="lede">` 都不渲染），
  编辑模式把它造出来摆「＋ 新建导语」。点一下从零新建，取消则占位原地补回。
  首页索引里那行简介同样支持。
- 编辑中一次只改一段：改着的段落之外链接点了不跳（防整页刷新丢字）；
  `Esc` 退出模式（手上改着一段时先归那段管）；局部刷新换页模式保持，新页面重新描点。
- 工具条新增「编辑板块名 / 编辑文章标题」两档单行内联编辑，保存后轨道栏、
  首页索引、卷帘、页头与标签页当场全部跟上；提示条照旧挂一次撤销。
- 新增自检 `tools/globaledit-check.mjs`（36 项：访客不可见 · 点字即改 ·
  空位新建（首页与动态页）· 标题内联改 · Esc 退出 · 跨页模式还在）。

### 变更

- 全站外壳（`server/lib/shell.mjs`）与手写的 `about.html` 命令栏加入全局编辑按钮；
  静态页已重新生成（`node tools/build.mjs`）。
- `sections.js` 的同步对齐跳过正在编辑的段落与挂着「＋ 新建」占位的框，不再覆盖输入中的内容。
- `studio.js` 把「页面上直接改字」的机器抽成 `cv01.direct` 供全局编辑模式复用；
  一段编辑结束会广播 `cv01:edit-ended`。

## 1.1.x - 2026-09-16

- 站长右键菜单：改名 / 撤下 / 恢复（原生的进覆盖层，界面建的真删）、
  页面上直接改字（像 Word 那样：四档字号、文字效果、emoji、Ctrl+S）。
- Markdown · 公式 · emoji 一条管线（`server/lib/markdown.mjs`）；
  编辑页正文与页面上改的那版富文本之间的让位规则。
- 门厅（login.html）：访客直接进，站长要口令；上传服务、音乐盒、云村。

## 1.0.0 - 2026-09-15

- 站点骨架：钢琴卷帘目录、九板块九轨道、生成器（`tools/build.mjs`）与全站外壳、
  配色系统（`content/palette.mjs` 唯一真源）。
