# 更新日志

本站的版本线。次位加一是有新功能进场，末位加一是修修补补。
（1.1 及之前的历史按提交归纳，细节见对应的提交与 README。）

## 1.3.0 - 2026-09-17

### 新功能

- **口令试错限速**：口令错头 5 次不罚，之后每错一次的等待时长翻倍（2s → 4s → 8s…），
  封顶 15 分钟；等待期内重试回 429 并带 `Retry-After`。口令输对即清空该记录。
  限速按来客计数——默认只认本机地址，挂 tunnel 时用 `CV01_TRUST_PROXY=1`
  才按真实 IP，启动横幅会把当前模式印出来。记录只在内存里，重启即清空。
- **口令可以换，而且不用手打**：`tools/set-passphrase.mjs` 生成一条 32 位随机口令
  （约 186 bit 熵）写进 `data/settings.json`；`tools/copy-key.cmd` 把它放进剪贴板，
  门厅那一栏 `Ctrl+V` 就进去了。首次启动自动生成的口令也从 8 位十六进制提到了 32 位。
- **上线前自检** `tools/preflight-check.mjs`：读配置（口令长度、门厅开关、凭据有没有
  跑进仓库）+ **真的去敲每一页**（不盖章能不能读到、服务端数据与源码是不是 404、
  写接口不带口令是不是 401），并把当前设计上公开可读的接口列出来让人确认一遍。
- 限速自检 `tools/authlimit-check.mjs`（19 项纯逻辑用假时钟跑完几小时的退避 + 5 项真接口）。
- `deploy/` 长期部署手册（Cloudflare Tunnel + Access 分步流程、开机自启、速查表）
  与幂等的后台源站启动脚本；`-PublicDeploy` = 不启动云村小服务 + 限速按真实来客。

### 修复

- **`.ncm-session.json`（网易云登录态）曾经能被公网直接 `GET` 走**：静态服务挡的是
  目录名白名单，拦不住站点根目录下的单个点文件。现在任何一段以 `.` 开头的路径都不外发，
  `deploy/`（那里的后台日志开头就印着口令）也进了名单。是上线前自检真敲出来的，不是读代码猜的。
- 口令比对从 `===` 改成定长比较，不再按第一个不同的字节短路。
- `/api/render`（不要口令是设计如此，编辑页预览靠它）加了闸：正文上限 256KB、
  同时在渲染最多 2 个，超了 429——它不落盘，但白算 CPU。

### 文档

- README 新增「这么长我自己怎么登录（不用手打）」与「挂到公网之前：先跑一次预检」；
  口令一节补上轮换与限速的来龙去脉。
- `deploy/CLOUDFLARE-TUNNEL.md` 第 9 节：门厅留着当「开屏过场」、Access 盖在前面，
  两层各回答什么问题。

## 1.2.0 - 2026-09-16

### 新功能

- **全局编辑模式**：命令栏右侧新增「全局编辑」按钮（服务在线且有口令的浏览器才看得见），
  一按整站进入编辑模式——虚线框里的文字**点一下就直接改，全程无需右键**：
  板块页的板块名 / 简介 / 导语，文章页的标题 / 正文，任何页面上的轨道栏板块名、
  首页索引（板块名与简介行）、卷帘轨道头、文章行标题。
- **空简介 / 空导语就地新建**：板块简介没写过（含动态板块页的「新建板块」占位）时，
  框里摆「＋ 新建简介」；导语整段缺失时（动态页连 `<p class="lede">` 都不渲染），
  编辑模式把它造出来摆「＋ 新建导语」。点一下从零新建，取消则占位原地补回。
  首页索引里那行简介同样支持。
- 编辑中一次只改一段：改着的段落之外链接点了不跳（防整页刷新丢字）；
  `Esc` 退出模式（手上改着一段时先归那段管）；局部刷新换页模式保持，新页面重新描点。
- 工具条新增「编辑板块名 / 编辑文章标题」两档单行内联编辑，保存后轨道栏、
  首页索引、卷帘、页头与标签页当场全部跟上；提示条照旧挂一次撤销。
- 新增自检 `tools/globaledit-check.mjs`（36 项：访客不可见 · 点字即改 ·
  空位新建（首页与动态页）· 标题内联改 · Esc 退出 · 跨页模式还在）。

### 变更

- 全站外壳（`server/lib/shell.mjs`）与手写的 `about.html` 命令栏加入全局编辑按钮；
  静态页已重新生成（`node tools/build.mjs`）。
- `sections.js` 的同步对齐跳过正在编辑的段落与挂着「＋ 新建」占位的框，不再覆盖输入中的内容。
- `studio.js` 把「页面上直接改字」的机器抽成 `cv01.direct` 供全局编辑模式复用；
  一段编辑结束会广播 `cv01:edit-ended`。

## 1.1.x - 2026-09-16

- 站长右键菜单：改名 / 撤下 / 恢复（原生的进覆盖层，界面建的真删）、
  页面上直接改字（像 Word 那样：四档字号、文字效果、emoji、Ctrl+S）。
- Markdown · 公式 · emoji 一条管线（`server/lib/markdown.mjs`）；
  编辑页正文与页面上改的那版富文本之间的让位规则。
- 门厅（login.html）：访客直接进，站长要口令；上传服务、音乐盒、云村。

## 1.0.0 - 2026-09-15

- 站点骨架：钢琴卷帘目录、九板块九轨道、生成器（`tools/build.mjs`）与全站外壳、
  配色系统（`content/palette.mjs` 唯一真源）。
