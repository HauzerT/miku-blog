# 设计志 · 初音ミク CV01

> 站点长成现在这样的完整记录：概念、色彩、字体、版面，到三轮换色的每一步判断，
> 再到配色变成生成物之后的轮次与窗；后面三节是窄屏读法、已知取舍与设计资产。
>
> 视觉哲学宣言《校准之息》在根目录 [README.md](README.md#设计哲学--校准之息calibrated-breath)；
> 怎么跑这个站、怎么发布，见 [HANDBOOK.md](HANDBOOK.md)。

## 目录

- [设计计划](#设计计划) —— 概念、色彩、版面与三轮换色的完整设计记录。
  **注意这一节记的是「当时怎么想的」，不是当前站点结构**：文中凡是提到「零构建」
  「双击 `index.html`」「`tools/build.mjs`」「生成出来的 `*.html`」的句子，讲的都是
  1.x 那条已经删掉的线（见第 14 节）。
- [设计系统速查](#设计系统速查) —— 改颜色只动 `content/palette.mjs`（[窗：卷帘那一扇](#窗卷帘那一扇)）
- [手机（≤900px）](#手机900px) —— 窄屏是另一套读法
- [已知取舍](#已知取舍)
- [设计资产](#设计资产) —— 海报与预览图

## 设计计划

> 设计的完整底稿，全文并入自 `design/DESIGN-PLAN.md`：从概念、色彩、版面，到三轮换色的
> 每一步判断，以及对照「生成式默认套路」的自我审校。文中「第 N 节」指本节内部的小节编号；
> 后面的节是对前面节的修订与续篇——历史冲突，以后面的节为准。
>
> **这一节记录的是「当时怎么想的」，不是当前站点结构。** 里面凡是提到
> 「零构建」「双击 `index.html`」「`tools/build.mjs`」「生成出来的 `*.html`」
> 「`server/lib/shell.mjs`」的句子，讲的都是 1.x 那条线——**它已经删掉了**（见第 14 节与
> [README.md](README.md) 顶部的说明）。当前结构只以 [README.md](README.md)、
> [HANDBOOK.md](HANDBOOK.md) 与代码为准。

### 0. 简报解读

| 项 | 判断 |
|---|---|
| 主体 | 初音未来（CV01）主题的个人博客骨架，作者是中文写作者 / 开发者 |
| 观众 | 作者本人 + 同好（ACG、练题、剪辑、自托管圈子） |
| 首要任务 | 让人**一眼认出这是初音**，同时相信这里真的能长期写东西 |
| 交付 | 零构建、双击即开的静态站点骨架（10 个页面）+ 一套可复用 token |
| 约束 | 不使用任何第三方插画 / 素材；全部视觉由 CSS + SVG 原创生成 |

题面给的七个板块名本身带着很强的中文网络语感（doge、囚徒、薯条、剪剪又辑辑、胡盐乱雨）。
它们**不是**七个并列的文件夹，它们有情绪高低。这一点决定了整个设计：

> **七个板块 = 一个和弦里的七个音。**

F#3 · A3 · C4 · E4 · G4 · B4 · D5 —— 堆叠三度，构成一个 F#m11。
越往下越重（做题、囚禁），越往上越轻、越抒情（云上、千世界、胡盐乱雨）。
这条隐藏线索同时是导航、是编号系统、是首页最显眼的那个东西。
懂乐理的人会愣一下；不懂的人只觉得这条上升的线很好看。两者都成立。

### 1. 概念

**调声台 (The Tuning Desk)**——博客不是什么"卡片流"，它是一台人声合成器的工程界面：

- 首页主视觉 = 一条**钢琴卷帘 (piano roll)**。七条轨道 = 七个板块，音符块 = 文章。
- 音高名 (F#3) 取代 01/02/03 序号。**编号本身携带信息**（板块的轻重），不是装饰性计数。
- 播放头（琥珀色竖线）= 唯一允许动的东西，也是"你现在所在位置"的指示器。
- 文章页顶部保留一小条卷帘，把当前文章所属的音符点亮——它同时是面包屑和定位器。

### 2. 色彩（6 个核心 token，每个都有职务）

> 第二轮修改：按客户要求改成「青底粉字」——底色用初音标志青，文字用粉。
> 这是这套设计里唯一一次色相整体反转（见第 11 节）。

| Token | Hex | 职务 | 备注 |
|---|---|---|---|
| `--paper` | `#39C5BB` | 青：初音标志色，整站地面 | 客户指定的标志色，实测渲染像素 100% 命中 |
| `--ink` | `#6E0B3E` | 深玫瑰：正文、结构线 | 对青底 5.55:1，过 AA |
| `--miku-deep` | `#7E0B45` | 玫瑰·签：地面上的链接、序号、音高名 | 4.89:1 |
| `--miku` | `#FF9ECB` | 樱粉：**只**出现在深色暗窗上 | 对暗窗 9.88:1；对青底只有 1.65:1，禁止上地面 |
| `--display` | `#071316` | 暗窗。卷帘带、页脚 | 全站唯一的深色区域，"仪器窗口" |
| `--cuer` | `#F5A623` | 示。"此刻" | 播放头、悬停读数、焦点环。全站唯一暖色 |

派生：`--paper-deep rgba(7,19,22,.10)`（悬停/内嵌码）、`--ink-soft #7A2350`（次要文本 4.52:1）、
`--rule rgba(110,11,62,.32)`（细线，带墨色倾向而不是灰）。
暗色主题（"夜间调声"）保持原样：`--paper→#0A1416`、`--ink→#DCEBEA`、签色回到青绿 `#39C5BB`。

**颜色即语法**：玫瑰=结构与所指，樱粉=被指认，琥珀=此刻，青=地面。任何一个颜色出现在错的位置都算 bug；
"樱粉出现在地面上"这一条是硬性禁止，它在那里不可读。

### 3. 字体（3 个角色，按功能分工而非按大小分工）

| 角色 | 字体 | 用途 |
|---|---|---|
| **读** (reading) | Noto Serif SC / 思源宋体 → 系统衬线回退 | 文章正文、板块标题。中文长文用衬线，`1.85` 行高 |
| **操作** (operating) | Noto Sans SC / 苹方 / 微软雅黑 | 界面微标签、按钮、说明行 |
| **测量** (measuring) | **Big Shoulders**（随站自带，OFL） | 拉丁字标、音高名、Hz/BPM 读数、日期。窄体、工业感、柱状垂直张力 |

三者共享一个隐含栅格；Big Shoulders 只承担拉丁与数字，中文永远不会掉进它里面。
**没有等宽字体**——"小数据标签用等宽"是典型的生成式套路，这里用窄体承担读数反而更贴合仪器面板。

#### 字阶（含中文行高修正）

| 名称 | size | line-height | 用途 |
|---|---|---|---|
| `--t-hero` | `clamp(2.6rem, 7.5vw, 5.2rem)` | 0.92 | 字标 / 板块大标题 |
| `--t-section` | `clamp(1.7rem, 4vw, 2.6rem)` | 1.15 | 板块页标题 |
| `--t-post` | `clamp(1.35rem, 2.4vw, 1.9rem)` | 1.3 | 文章标题 |
| `--t-body` | `1.0625rem` (17px) | 1.85 | 中文正文，行宽 ≤ 34 汉字（≈ 34em） |
| `--t-ui` | `0.8125rem` | 1.5 | 界面标签，`letter-spacing: .02em` |
| `--t-data` | `0.75rem` | 1.2 | 读数、刻度、坐标标注 |

### 4. 版面

#### 首页

```
┌───────────────────────────────────────────────────────────────────────┐
│ 初音ミク CV01          索引  归档  关于            [调声/夜间]  ────────│ 56px 命令栏
├───────┬───────────────────────────────────────────────────────────────┤
│       │  HATSUNE MIKU                      7 TRACKS · F#m11 · 120BPM │
│  F#3  │ ████████████████████ 暗窗：卷帘 ██████████████████████████████│
│  A3   │  ♪ 做题区doge   ♪♪ 网上囚徒    ♪ 整点薯条                      │
│  C4   │        ♪ 剪剪又辑辑      ♪ 生活在云上                          │
│  E4   │   ♪♪ 千千千世界        ♪ 胡盐乱雨集        ▏播放头→            │
│  G4   ├───────────────────────────────────────────────────────────────┤
│  B4   │  七板块索引（不是卡片，是七行带细线的版面条目）                 │
│  D5   │  ── F#3 ─ 做题区doge ─────────────────────  12 篇 · 最近 →     │
│       │      一句话说明 + 最近两篇标题                                 │
│       │  ── A3 ── 网上囚徒 ───────────────────────  8 篇 · 最近 →     │
└───────┴───────────────────────────────────────────────────────────────┘
   左侧琴键轨道 = 板块导航（竖排中文标签，桌面端常驻；移动端收起为顶部横向条）
```

**对齐**：全站左对齐。轴心是左侧那条竖向琴键轨道（`--rail: 96px`）——正文、标题、细线全部从这条轴出发，右端参差。这带来一种"所有东西都挂在同一根弦上"的秩序感。只有页面最底部一行小字允许居中（收尾的休止符）。

#### 板块页

```
命令栏 → 小卷帘条（当前音符高亮，琥珀游标停在其起始处）
── 板块标题（衬线大字）+ 音高读数 + 一句话定义 ──────────────
  导语：2–3 行，说明这里写什么、不写什么
  ─────────────────────────────────────────────────────────
  文章条目 × N：日期(窄体) · 标题(衬线) · 一句话摘要
  条目之间 1px 细线，无卡片、无圆角、无阴影
```

#### 文章页

```
卷帘定位条（所属音符点亮）
标题
日期 · 板块 · 阅读时长      ← 一行读数，用 · 连接是允许的（它是真实元数据）
──────
正文 ≤34 汉字行宽，衬线，段间距 1.35em，小节标题带左侧 2px 青绿短标尺
文末：上一篇 / 下一篇（各带音高名）
```

### 5. 结构性装置（以及为什么）

| 装置 | 理由 |
|---|---|
| 钢琴卷帘作为主导航 | 直接来自"初音=人声合成器"这一事实，不是通用组件 |
| 音高名代替序号 | 序号只在内容真是序列时才有意义；这里的序列是**音高**，它真的携带信息 |
| 一条贯穿的暗窗 | 全站唯一的深色区域，把"boldness 花在一处" |
| 竖排中文板块标签 | 中文传统排版，且解决窄轨道的横向空间问题——功能性，不是异国情调 |
| 卷帘定位条 | 同时是面包屑、当前位置指示器、和主视觉的回声 |
| 细线规则 | 内容真的是条目列表，细线是列表语法的可视形式 |

**主动避开**：等宽小标签、全大写 eyebrow、每个 heading 上方的分类小字、圆角卡片群、`rgba(0,0,0,.1)` 统一阴影、渐变洗色、`→` 后缀、居中大数字 + 副标签的 hero、编号 01/02/03、`A · B · C` 元数据串滥用（仅文章页保留一处真实元数据）。
刻度线、平行细线、音符块三类重复图形承担"系统性观测"的密度——重复本身是设计，不是填充。

### 6. 动效

**唯一一次编排**：页面载入时，琥珀播放头用 2.2s 从左扫到右，经过的音符块依次亮起 120ms，然后停住；此后全站再无自动动画。
交互反馈（悬停抬起音符、聚焦描边、主题切换 180ms 颜色过渡）都是"回答用户动作"的动效，允许存在。
`prefers-reduced-motion: reduce` → 播放头不扫，音符一次性呈现，过渡归零。

### 7. 工艺底线

响应式到 360px（卷帘横向滚动 + 吸附，琴键收起）；键盘可达（卷帘支持 ←/→ 与 Home/End，全部链接有 `:focus-visible` 琥珀描边）；AA 对比度；打印样式；无布局位移 (CLS)；无第三方请求、无构建、`file://` 直接可开。

### 8. 对照"生成式默认套路"的自我审校

| 常见默认 | 本方案 |
|---|---|
| 暖奶油底 + 高对比衬线 + 陶土色 | 冷青白纸 + 蓝黑墨 + 官方青绿；暖色只保留一丁点琥珀，且**只表示"此刻"** |
| 近黑底 + 单一酸绿强调 | 亮纸为主（≥70%），暗色只作一条窄窗 |
| 报纸式通栏细线 + 零圆角 | 有细线，但不是报纸分栏；主导结构是卷帘轨道；音符块保留 2px 圆角（它是"块"，需要被认成块） |
| SaaS 卡片套件 | 全站零卡片、零统一圆角、零统一阴影 |
| 通用模板外壳（eyebrow / · 串 / →） | 无 eyebrow；`·` 只在文章元数据出现一次；链接不加箭头，改为悬停时音高读数更新 |
| 每个 section 淡入上移 | 只有一次播放头扫描，且它表达"播放"这一真实含义 |

**审校后改掉的两处**：
1. 初稿让七个板块按 01–07 编号 → 改为音高名。序号是模板肌肉记忆，音高才是这台机器本来的语言。
2. 初稿 hero 用"巨大字数 + 说明 + 渐变"（因为"最近更新篇数"很诱人）→ 删掉，改为卷帘本身承担 hero。数字退到板块条目的行末，作为一行小读数。

### 9. 已知取舍

- 卷帘在 360px 下必须横向滚动，这是承认真实约束（七条轨道无法在手机上同时铺开），用吸附 + 滚动提示处理，而不是硬塞成列表。
- 衬线中文在 Windows 上走系统字体（Noto Serif SC / 宋体），不同机器渲染会有细微差异；这是零构建的代价，本文里写明。

### 10. 建完之后回头改的地方（第三遍）

对着截图与像素测量复核后，动了这些——都是**减法或修正，没有加装饰**：

| 改动 | 原因 |
|---|---|
| 音符块高度 62% → 54%，加 1px 内描边 | 原尺寸在 46px 轨道里显得像甘特图色块，压扁后间隙出来了，"音符"的身份才立住 |
| 卷帘底纹增加"隔小节淡带" | 只有竖线时读起来是甘特图；交替明暗才是卷帘 |
| 移动端 hero：轨道 46 → 32px、隐藏键盘列、轨道名收窄到 5.75rem、内宽 51 → 30rem | 原方案下 152px 的轨道名列会吃掉窄屏近一半宽度，可见音节只剩一两个 |
| 迷你定位条的"幽灵音符"不透明度 0.16 → 0.26 | 0.16 时完全看不见，失去了"我在整首歌哪一段"的作用 |
| 文章页翻页器加 `max-width: var(--measure)` | 原来横跨 1400px，"下一首"离正文列八百多像素，轴线断了 |
| "可以左右滑动"提示只在 <520px 出现 | 原来 <900px 就显示，但 520–900px 之间根本不会滚动——文案在说谎 |
| 海报频谱区从"竖条高度编码强度"改为"水平谐波条纹" | 前者是条形图，不是频谱。改成水平条纹后共振峰自然浮现，且与上方卷帘严格同一时间轴 |
| 海报右上补一块标本数据（7 VOICES / 64 BEATS / F#3–D5 / 185–587 Hz） | 标题右侧原本是一整块空白；补的是真数据，同时压住标题的巨大体量 |

复核手段：headless Chrome 截图 + PIL 逐像素测量（不是肉眼估）。
测量确认：桌面轨道 46px、音符块 25px、迷你条 22px、高亮始终落在正确的行；
海报频谱中七条青绿基频条纹反算频率与标准音高偏差 +1…+5 音分。
另核对全站 32 个页面、667 条站内链接与资源，全部存在。

### 11. 第二轮修改：九轨 / 青底粉字 / 每日一句

客户提了三件事，逐条记下判断：

**（一）加两个板块：二次生命（二次元）、恰同学少年（同学回忆录）。**
顺着原来的堆叠三度继续往上排，新音是 **A5（880.00 Hz）** 与 **F#5（739.99 Hz）**，
音域变成 F#3 – A5。原有七个板块的音高一个没动——已经发出去的链接、卷帘上的位置、
海报里的基频全部保持有效。轨道数从 7 变 9 之后，**没有任何一处布局需要手改**：
卷帘、轨道栏、"九板块索引"、篇数统计都从 `tracks.length` 推出来。

**（二）背景改青、文字改粉。**
照做了，但这里有一个必须说清的物理约束：青 `#39C5BB` 的相对亮度是 0.445，
它上面**只可能**放深色文字——浅粉（`#FFD9E8`）实测 1.65:1，白字上限也只有 2.1:1。
所以我把"粉色"拆成两级：
- **深玫瑰 `#6E0B3E`** 承担正文与结构（5.55:1，实测像素 `#6D0B3E`）；
- **亮樱粉 `#FF9ECB`** 只出现在深色暗窗上（9.88:1）——卷帘的音符块、迷你定位条的当前轨。
这样"青底粉字"在字面上成立（青是标志青，字是粉），在阅读上也不塌。
`tokens.css` 顶部另留了一组更护眼的替代值（浅底亮粉，6.2:1），客户若不适应满屏饱和青，一行就能换。
夜间模式按要求**一点没动**。

**（三）胡盐乱雨集的文字在每页顶上随机显示，每 24h 换，小字。**
做成一条 `.epigraph` 页眉：`.8125rem` 衬线小字，标签「胡盐乱雨集」链到该板块。
"随机"用的是**本地日期做种子**，不是每次刷新随机——否则同一天里翻三页会看到三句不同的，
那不是"每日一句"，那是闪烁。句库在 `content/posts.mjs` 的 `excerpts`（14 条），
由生成器编译成 `assets/js/excerpts.js`，避免在 38 个页面里各内联一份。
（这一段是 1.x 的记录：那个文件与它的生成器都已经删掉，现在由 `composables/useExcerpt.ts`
按同一套散列直接算，句库仍然来自 `content/posts.mjs` 的 `excerpts`。）
无 JS 时页面里预渲染首句，不会空着。

**海报同步换色**。它是同一套视觉 DNA 的另一个视图，不能留在旧配色里。
换色时顺带解决了一个新问题：青底上琥珀（`#F5A623`，对青底 1.05:1）不可读，
所以观察场里"此刻"的游标改用最深的玫瑰，而琥珀只留在暗窗的播放头上——
两者横坐标完全相同，指向同一个瞬间。频谱的等级差改由**墨量**表达（基频最深，泛音压到 0.28 以下），
因为青底上已经不能靠色相区分主次了。

改完后重新量了一遍：底色实测 `#39C5BB`（正文区 86% 像素命中），正文实测 `#6D0B3E`，
青/粉组合 5.55:1；38 个页面、站内链接与资源全部存在。

### 12. 第三轮修改：中性地面（本节取代第 2 节与第 11 节的色彩部分）

客户给了一套完整的调色板，要求整站换上去：

```
mikuGreen  main #39C5BB   light #86CECB   dark #137A7F
mikuPink   main #FF3399   deep  #E12885   light #FF69B4
亮  bg #F5F8F8  card #FFFFFF  text #2D3033  text2 #666A6D  border #BEC8D1  mark #FFE6F2
暗  bg #1A1C1E  card #2D3033  text #E6EBEF  text2 #9DA4AA  border #44494D  mark #4A2038
```

这套板子的结构和本站原来的恰好相反：它是**中性底 + 卡片**，本站是**饱和青当整块地面、零卡片**
（第 2 节 / 第 8 节）。判断是：客户换的是「地」，不是「骨架」。

**（一）只换值，不换名。** `tokens.css` 的 token 名一个没动（`--paper` / `--ink` / `--miku` /
`--miku-deep` / `--display` / `--on-display` …），所以 7 个 CSS 文件、40 多个页面一行都不必重写。
逐条职务映射写在 `tokens.css` 顶部注释里。

**（二）调色板没定义的职务，自己补。** 来源板子只给了「地 / 面 / 字 / 线 / 选区」，
本站还要「签」和「暗窗」：

- **签**照旧拆两级，只换色相：`--miku-deep`（地面态）取 mikuPink，`--miku`（暗窗态）取
  mikuGreen.main。于是「粉 = 被指认、青绿 = 暗窗内的读数」这条语法保住了，只是青与粉换了位置。
- **暗窗**（`--display`）取来源的暗色中性：亮色主题下 `#1A1C1E`，暗色主题下再往里凹一档 `#0C0E10`，
  `--display-soft` 取暗色 `cardBg #2D3033`。卷帘、页脚、代码块、视频屏仍是全站唯一的深色区域，
  第 5 节「把 boldness 花在一处」没有被破坏。
- **`cardBg` 落到浮层上**：工作台面板、口令弹窗、两颗悬浮球改用新 token `--card`
  （亮 `#FFFFFF` / 暗 `#2D3033`）。第 8 节「零卡片、零统一圆角、零统一阴影」说的是不做**卡片流**；
  浮层不是卡片流——它们本来就各有边框，这次只是底色从 `--paper` 换成 `--card`。
- **`markBg` 落到选区**：`::selection` 由「樱粉底 + 暗窗色字」改为 `--mark` + `--ink`。

**（三）两处偏离来源调色板，都是量出来的。**

1. **粉的深色档压深一格。** `#E12885` 对 `#F5F8F8` 实测 **4.06:1**，差 0.44 到 AA 的 4.5。
   `#D81B7A` 同色相压深一档得 **4.51:1**，过线，肉眼几乎分不出。正文另用中性 `#2D3033`
   （12.43:1），粉只承担链接 / 序号 / 音高名这类短标签。
2. **「此刻」由琥珀换成 mikuPink.main。** 琥珀 `#F5A623` 对旧的青底只有 **1.05:1**——
   第 11 节记过这件事，但当时只把海报上的游标换掉了，**站内那条焦点环其实一直是隐形的**。
   换到中性地面，琥珀也只有 **1.90:1**，仍然不合格。而来源板子里没有第三个色相的位置：
   青绿 `#39C5BB` 对地面 **1.99:1**（它天生只配活在暗窗里），粉 `#FF3399` 对地面 **3.18:1**
   （够 WCAG 2.2 焦点指示器要求的 3:1）、对暗窗 **5.03:1**（够正文）。于是全站回到两组色系 + 中性灰，
   琥珀退场；暗色一套里「此刻」用 `#FF69B4`（对 `#1A1C1E` **6.45:1**）。
   收益是实的：焦点环第一次真的看得见。

**（四）「颜色即语法」在这一轮反而更严了。** 地面变中性之后，等级差不能再靠色相，必须交给墨量——
这正是哲学第 3 节里「最容易被违反、也最值得守住」的那条。现在：青绿只出现在暗窗内部
（音符块、读数、当前轨），粉只用来指认（链接、序号、音高名），灰承担全部阅读重量，
「此刻」是全站唯一会动的颜色。

**（五）怎么验的。** 全站页面的 `theme-color`（`nuxt.config.ts` 与两张壳）、
卷帘组件吐出来的行内色、`composables/useTheme.ts` 的主题切换、favicon、二维码默认色全部同步。
用无头 Chrome 抓的是**计算之后的渲染色**，不是 token 表：（那一轮量的时候还没有 Nuxt，
用的是后来已经删掉的那批静态页与 `tools/dom-check.mjs`——数字是留档，不是现在能重跑的命令。）

| 职务 | 亮色实测 | 暗色实测 | 对比度（亮 / 暗） |
|---|---|---|---|
| 地 · 墨 | `#F5F8F8` · `#2D3033` | `#1A1C1E` · `#E6EBEF` | 12.43 / 14.24 |
| 签·地面 | `#D81B7A` | `#FF69B4` | 4.51 / 6.45 |
| 签·暗窗（音符块） | `#39C5BB` 底、`#1A1C1E` 字 | `#39C5BB` 底、`#0C0E10` 字 | 8.04 / 9.10 |
| 暗窗 | `#1A1C1E` | `#0C0E10` | — |
| 此刻（播放头 / 焦点环） | `#FF3399` | `#FF69B4` | 3.18（图形）/ 6.45 |

`node tools/dom-check.mjs`（**这个脚本已经随 1.x 静态线删掉了**，数字留档）：40 项里 39 项通过。唯一那项是 `/favicon.ico` 404——页面只声明了
`favicon.svg`，浏览器仍会自动去要 `.ico`（改动前就存在，与配色无关）。

**没有跟着换的东西**：`design/keyvisual.html` 及其 PNG / PDF。那张观测图是按哲学第 3 节做的独立作品，
青底就是它的立论本身；要换得重新导出。（这句到此为止，见第 17 节。）

### 13. 配色成了生成物：唯一真源、轮次与窗（本节更新第 2 节的色值表与第 12 节的手工映射）

第 12 节那场换色是**手工**做的：四十多个 token、七份 CSS、四十多页 `theme-color`，一处一处对过去。
同样的活儿干第二轮就会出错，于是它被做成了机器：

- **`content/palette.mjs` 是全站唯一写着颜色字面量的地方。** 四层自下而上：
  `paint`（原色，有名有出处）→ `tokens`（职务：谁挂在谁身上、至少多少对比度）→
  `rounds`（一轮完整配色 + 一段为什么）→ `windows`（卷帘那一扇自己的一套）。
  别的 CSS 里出现 `#rrggbb` / `rgba()` 都是 bug——`tools/tokens.mjs` 会逐个揪出来。
- **对比度是校验项，不是文档。** 生成器逐对重算，不够 AA 直接报错退出、不写文件。
  所以 `palette.css` 顶上那张实测表永远和实际颜色对得上——第 2 节那种手写色值表从此退役。
- **轮次**：r3 中性地面（当前线上）、r4 青墨归位（候选）、r5 四色归位（候选）。
  换整套配色 = 改 `active` 一个字；浏览器加 `?cv01-palette=r4` 不动文件就能并排比，
  `node tools/tokens.mjs --list / --diff r3 r4 / --use r4` 在命令行做同样的事。
  **历史轮次冻结**：r3 / r4 记录的是已经发生过的决定，不该被后来的口味改写。

**窗（`--win-*`）**：卷帘是画面里唯一的"另一个地方"，所以它不跟页面的四色走，自己一套。
两个方案一行切换：

| 窗 | 是什么 |
|---|---|
| `ink` 墨窗 | 窗的地就是页面的暗色（只写 `var(--display)` 别名），页面换轮次窗跟着变——原来那一扇 |
| `cyan` 青窗（当前） | 白天青底黑键白字；晚上整块翻成深底粉键黑字 |

青窗夜里为什么要翻面：粉 `#FF3399` 落在青 `#39C5BB` 上只有 **1.60:1**，粉块会整块消失
（黑边救形状不救颜色）；近白也只有 2.13:1。青窗里只留得下黑——这是算出来的，不是选的。
顺带的两条推论，都进了生成器：

1. **窗里的线必须是黑的。** 原来那几根 `--roll-line` 是青 alpha，铺在青地上整片消失。
2. **黑必须中性，不染青。** 黑一旦偏青就跟青同族，青在它上面只剩明度差、没有彩度差，
   画面从四个颜色塌成三个。黑越纯，青越亮。

**卷帘场里没有网格了。** 原来铺着三层 `repeating-linear-gradient`（小节线 / 拍线 / 隔小节淡带），
一铺上去整块像坐标纸，抢的正是音符的对比。时间轴改由小节号和音符自身的位置与宽度承载；
行与行的界线留在轨道上——那不是网格，是"这个音符属于哪条轨道"的归属线。

### 14. 交付形态变了：10 页骨架长成 42+ 页加一台服务（本节更新第 0 节「交付」与第 7 节两条底线）

第 0 节写「交付：零构建、双击即开的静态站点骨架（10 个页面）」。它如今读作：

| 第 0 节的原文 | 现在的情况 |
|---|---|
| 10 个页面 | 42+ 页（九板块 + 文章 + 归档 + 编辑页 + 门厅 + 云村 + …） |
| 零构建、双击即开 | **已经作废**：v2.0.0 起主线是 Nuxt 应用，这一次的整合又把 1.x 的零构建静态线整个删掉了。现在是 `pnpm install && pnpm build`，再 `node .output/server/index.mjs` |
| 无第三方请求 | 修正为：仅 `assets/vendor/` 里**两个本地库**（marked + KaTeX，无 CDN）；其余全部手写 |
| 纯静态 | 修正为：一台 Node 服务（Nitro）提供页面与接口，上传 / 编辑 / 门禁 / 云村都在它上面 |

新增的目录各有各的职务：`server/`（Nitro 接口与数据层）、`pages/` 与 `components/`（Vue 页面）、
`data/` 与 `media/`（界面产生的东西，不进仓库）、`content/palette.mjs`（配色真源，见第 13 节）。

第 7 节的工艺底线追加三条服务期的：

- **门禁**：服务跑着时未盖章访问任何页面 302 回门厅（`cv01-enter` cookie，三十天）；
  静态资源与接口不拦，否则页面自己都加载不出来。
- **每一次写接口都 `requireAuth`**：真正的权限是口令，门厅那枚章只回答"你是不是从门口进来的"。
- **页面一律 `no-cache`**（带 ETag）：不然浏览器拿一小时前的缓存就等于绕过了门。

### 15. 服务期的界面装置（第 5 节那张表的续篇）

| 装置 | 理由 |
|---|---|
| 门厅两颗键（粉=访客，黑=站长） | 身份就是这把口令。访客键**当场清掉**已存的口令——补过的一道漏：早先不清，「访客」会带着站长权限进门。两颗键都描 2px 黑边：粉与青之间只有 1.6:1，**键的轮廓由黑给出**，不靠粉自己撑 |
| 两颗悬浮球（音乐球公开 / 站长球验口令） | 会改硬盘的事统一收进站长球：没口令的人打开音乐盒看到的是播放控件，看不到任何写按钮。浮层底色用新 token `--card`——第 8 节「零卡片」说的是不做**卡片流**，浮层不是卡片流 |
| 站长右键 + 覆盖层 | 原生内容只「撤下」不「删除」：`data/overrides.json` 记一笔 + HTTP 410 + 一颗撤销，**源文件一个字节不动**；界面建的才真删。本站没有表示「危险」的颜色——粉只管「此刻」——所以破坏性的那一项靠加粗加一条细线，不靠红 |
| 编辑页：一条正文管线 | `Markdown（GFM 全量）→ 抠出公式 → marked → emoji 短代码 → 公式装回去`。公式在**写入时**编译成 HTML + MathML：页面不跑 JS、不闪。预览与最终页面是同一个渲染器。中文的一个让步：裸链接后跟中文标点，在第一个标点处剪断——GFM 会把「，然后」吞进链接里 |
| 站内跳转 | 走前端路由，外壳与右下角那根 `<audio>` 从头到尾不死——**切板块歌不断**。卷帘播放头每次进新页面重扫一遍，这算白送的效果 |
| 云村（`/kumura`） | 走 eapi 不走 weapi（实测网易已关掉 weapi 通道）。二维码用约 300 行的**本地编码器**画——为一张二维码引第三方库，等于拆掉零依赖的底线；正确性用 OpenCV 反向实扫验证。登录 cookie 只落在小服务旁边，不进浏览器、不碰 localStorage |
| WebAudio 琴声（默认关闭） | 没有采样时现场合成柔和电钢琴，**任意音高现算**（十二平均律）——用界面新建板块挑 B5、C6 也当场出声，不存在「后期补一条音高表」 |

### 16. 手机上的命令栏折成两行（第 4 节「移动端收起」的细化）

第 4 节画的是桌面；量过 320px 之后，窄屏不是把桌面压扁，是换一套读法：

- **≤480px 命令栏两行**：上行铭牌 + 两个开关，下行四个导航平分整宽（每格约 72×44px）——
  「铭牌 + 四导航 + 两开关」在 320px 的一行里挤不下，挤出来的小字不如两行好点。
  481–900px 仍是单行。`--bar` 跟着变 `calc(5.5rem + 1px)`，轨道栏的粘顶位置才不会错位。
- **轨道栏横排粘顶**：九个音从左侧竖排改成顶部横排，继续粘在命令栏下面——读长文时随手换板块。
- **卷帘留给手指**：轨道高 36px，音符下垫一层透明可点区；「可以左右滑动」要**量过 `scrollWidth` 才说**
  ——520–900px 之间根本不会滚，文案不许说谎（第 10 节那条的延续）。
- **页脚给悬浮球让出通道**（球高 ×2 + 留白）：手机滚到底时版权两行从球上面经过，不被压住。
- **触摸的规矩**：`touch-action: manipulation` 砍掉双击缩放的 300ms；`@media (hover: none)`
  撤掉会粘着的 `:hover`；`viewport-fit=cover` + `env(safe-area-inset-*)`，横屏不钻刘海与圆角。

### 17. design/ 自己也同步了一次（2026-09-16）

第 12 节末尾那句「没有跟着换的东西：`design/keyvisual.html` 及其 PNG / PDF」**到此为止**：

- **海报换成现行配色（r3 中性地面 + 青窗），重新导出 PNG / PDF。** 手法与第 12 节换站内配色相同：
  **只换值，不换骨架**——标题、卷帘、频谱、注册十字、锚句、每一根线的坐标都没挪。
  立论随站点一起更新：不再是「青底」，而是「中性地面 + 一扇青窗」——青搬进窗里做音符块（8.83:1），
  粉走上来承担指认（深档 4.51:1），频谱的等级差从「青底上最深的玫瑰」改成「中性地上最深的墨」
  （第 12 节（四）的「墨量表达」在海报上也成立了）。观察场的游标回到樱粉——它对中性地面 3.18:1，
  够 2px 线的 3:1；窗里的播放头仍是黑的，两条线同一时刻、同一横坐标，各用各的场上读得了的颜色。
  顺手修了一处陈年账：角标 `PLATE 01 · 07` 改成 `09`——第二轮加板块时就该改的。
- **`preview-home.png` 用当前首页重拍**（1600 × 1250，亮态，播放头扫完之后的静帧）。
- **哲学加了第七节后记**：四色职务在中性地面上的新读法。
- 复核照旧是量的，不是看的：海报像素抽检七点全中
  （地面 `#F5F8F8` / 窗地 `#39C5BB` / 音符块 `#101214` / 黑键轨 `#32A8A0` /
  游标 `#FF3399` / 标题墨 `#2D3033` / 窗内 `#39C5BB`）；预览抽样命中上万青窗像素。

## 设计系统速查

**颜色即语法 —— 而语法只写在 `content/palette.mjs` 里。**

| 文件 | 是什么 |
|---|---|
| `content/palette.mjs` | 唯一真源。四层：`paint` 原色 → `tokens` 职务与校验规则 → `rounds` 轮次 → `windows` 窗 |
| `tools/tokens.mjs` | 校验 + 生成。对比度、完整性、颜色字面量、变量接线，四样都过才写文件 |
| `assets/css/palette.css` | 生成物。当前轮次 + 当前窗的颜色变量，顶上那张实测对比度表是算出来的，不是抄的 |
| `assets/js/palette.js` | 生成物。所有轮次的值 + 浏览器里换轮次的开关（窗不在这里，换窗只差两次命令） |

改颜色的标准动作：

```bash
# 1. 改 content/palette.mjs（加一轮、改一个原色、调一条校验，都在这儿）
node tools/tokens.mjs      # 2. 校验并生成。不够 AA 就报错退出，不写文件
                           #    生成物就是 Nuxt 页面加载的那两份，没有第三步
```

想在**真页面**上比两轮配色，不用改任何文件：地址栏加 `?cv01-palette=r4`，
控制台里 `cv01Palette.use('r4')` / `.list()` / `.use(null)` 回到当前轮次。
`node tools/tokens.mjs --list` 列全部轮次与窗，`--diff r3 r4` 看差在哪，
`--check` 只校验不写文件（提交前 / CI 跑这个），`--use r4` 把某一轮定为当前轮次。

**职务**——颜色是职务，不是气氛。`palette.css` 顶上那张表逐对算了对比度：

| 职务 | 管什么 |
|---|---|
| **地** | 承担全部阅读重量。重量交给灰度的差，不交给色相 |
| **墨** | 正文与结构线 |
| **签** | 只用来指认被选中的东西：链接、序号、音高名、音符块 |
| **示** | 只表示「此刻」——播放头、焦点环、进度。画面上唯一会动的东西 |
| **线 / 窗 / 静** | 细线 / 卷帘那一扇自己的一套（见下） / 不跟主题走的（二维码、打印） |

### 窗：卷帘那一扇

卷帘是全站唯一的"另一个地方"，所以它**不跟页面的四色走**，自己有 `--win-*` 一套。
两个方案，`content/palette.mjs` 里一行决定：

| 窗 | 是什么 |
|---|---|
| `ink` 墨窗 | 窗的地就是页面的暗色（`--win-ground: var(--display)`，只写别名）。页面换轮次，窗跟着变 |
| `cyan` 青窗 | 白天：青底 + 黑键 + 白字。晚上整块翻过来：深底 + 粉键 + 黑字 |

```bash
node tools/tokens.mjs --window cyan    # 换窗（页面的四色一个不动）
node tools/tokens.mjs --window ink
```

青窗为什么在晚上换个地：粉 `#FF3399` 落在青 `#39C5BB` 上只有 **1.60:1**——粉块会整块消失
（那条黑边只能救形状，救不了颜色）。所以"粉键"必须配深地。量出来是这样：

| | 白天 | 晚上 |
|---|---|---|
| 窗的地 | `#39C5BB` 青 | `#101214` 深 |
| 键（音符块） | `#101214` 黑 | `#FF3399` 粉 |
| 块上的字 | `#F5F8F8` 白 · **17.58:1** | `#101214` 黑 · **5.53:1** |
| 播放头 | 黑线（不发光） | 粉线（带辉光）· 5.53:1 |
| 被指认的读数 | 黑 · 8.83:1 | 青 `#39C5BB` · 8.83:1 |

卷帘场里**没有网格**。原来铺着三层 `repeating-linear-gradient`（小节线 / 拍线 / 隔小节淡带），
一铺上去整块就像坐标纸，而且抢的正是音符的对比。时间轴改由上面那排小节号和音符自身的位置与
宽度承载；行与行的界线留在 `.lane` 上——那不是网格，是"这个音符属于哪条轨道"的归属线。

现有三轮：**r3 中性地面**（当前线上）、**r4 青墨归位**、**r5 四色归位**（都是候选，
改一行 `active` 或加个地址栏参数就能看）。青 `#39C5BB` 对浅地面只有 2.0:1，做不了字——
但它的深档 `#137A7F` 有 4.78:1，做得了；而青色做「实心块」有 8.0:1。
它该在哪儿，由这些数字决定，不由偏好决定。

**字体三个角色**：思源宋体（读）/ 系统无衬线（操作）/ Big Shoulders（测量）。
等宽只用于代码，不用做标签。暗色主题（"夜间调声"）是另配的一套，不是反相。

**动效**：全站只有一次编排——载入时播放头扫过卷帘 2.2 秒，经过的音符依次点亮
（窗里那条线：白天黑、晚上粉）。`prefers-reduced-motion: reduce` 下不扫，音符一次性呈现。

**无障碍**：卷帘支持 `←` `→`（同轨内沿时间轴）、`↑` `↓`（换轨）、`Home` / `End`；
所有可交互元素有 `:focus-visible` 描边；正文对比度过 AA（亮态实测 12.43:1）。

## 手机（≤900px）

窄屏不是把桌面版压扁，是换一套读法：

- **命令栏在手机上折成两行**：顶级页面是四个（首页/云村/归档/关于），
  「铭牌 + 四个导航 + 两个开关」在 320px 一行里仍然挤不下。
  所以 ≤480px 时上行放铭牌与两个开关、下行四个导航平分整宽（每格约 72×44，
  比挤在一行里的小字好点得多）；481px 以上仍是单行。`--bar` 跟着变成
  `calc(5.5rem + 1px)`，轨道栏的粘顶位置才不会错位。
- **轨道栏留在顶上**：九个音从左侧竖排改成顶部横排，并继续粘在命令栏下面——
  读长文时随手就能换板块，不必滚回顶部。（单列下 `.shell` 从 grid 换成 block：
  grid 单列时轨道栏独占一行、行高等于自身，sticky 会退化成普通元素。）
- **卷帘留给手指**：轨道高 36px、音符块 25px，音符下面还垫了一层透明的可点区域，
  实际能点到约一格轨道那么高。播放头那 78% 的空盒子由 `.roll__field` 裁掉——
  否则手机上能一直往右滑进一片空白，桌面端还会平白多出一根横向滚动条。
- **「可以左右滑动」只在真能滑的时候出现**：量过 `scrollWidth` 才加 `.is-scrollable`（在 `useRoll.ts` 里）。
  宽一点的手机上卷帘并不需要滑动，那就不说这句。
- **悬浮球不会吃掉页脚**：两个球是 `position: fixed` 的，视口右下那一条永远被压着，
  而页脚正好落在里面——手机上滚到底时版权两行原本会被球盖住。现在页脚留出了这条通道
  （球高 ×2 + 留白，写在 `studio.css` 里跟着球的实际尺寸走），滚到底页脚从球上面经过。
- **触摸的规矩**：`touch-action: manipulation` 去掉双击缩放那 300ms 的等待，点击高亮换成樱粉色；
  偏小的可点区域用透明 `::after` 或行内 `padding-block` 垫到 36–48px（视觉一点不动）；
  `@media (hover: none)` 里把只在指针下才该出现的悬停状态撤掉（手指点过之后 `:hover` 会一直粘着）。
- **刘海与圆角**：`viewport-fit=cover` + `env(safe-area-inset-*)`，横屏时正文不会钻进圆角；
  `theme-color` 跟着主题走，地址栏不留一条白带。首帧那一份在 `nuxt.config.ts` 与两张壳的
  `useHead` 里发出，切主题之后再按当前配色重算一遍——改配色时这两处要一起改，
  否则地址栏会悄悄掉队。
- **横屏手机**（高 ≤480px）：命令栏、hero、轨道各收一档，别让标题把正文挤出首屏
  （只在命令栏还是单行时才压矮它，480px 宽的横屏机走的是两行布局）。
- **正文**：代码块与表格在窄屏自己滚，行内代码允许断行；行距 1.9、字号 17px 不动——
  手机上一行 30 来个字，正好。

## 已知取舍

- 中文衬线走系统字体（Noto Serif SC / 宋体），不同机器渲染有细微差异。
  想完全一致就把思源宋体的子集一起打包进 `assets/fonts/`。
- 窄屏上卷帘仍要横向滑动：九条轨道无法在手机上同时铺开。轨道名固定在左侧，
  320px 的机器上大约看得到三条轨道的三分之二——这是承认真实约束，不是没适配。
- 悬浮球固定在右下，手机上会压住正文最右侧约 58px（首页那条卷帘的右端也在其中）。
  这是悬浮球的固有代价；真嫌挡，可以让它在卷帘进入视口时淡出，或者手机上改成横排贴底。
- 中性地面比满屏饱和青好读得多（正文 12.43:1），代价是初音的标志青从「整站的地面」退到了
  暗窗内部——它现在只出现在卷帘、代码块、视频屏里。这一轮把「指认」交给了粉，于是粉越权了：
  它本来只该管「此刻」。候选的 `r4` 把指认还给青（青墨 `#137A7F`，4.78:1）、把暗窗挪成青黑
  `#0E2124`，粉退回焦点环与播放头。两轮都在 `content/palette.mjs` 里，加个
  `?cv01-palette=r4` 就能并排看，选好了改一行 `active`。
- **撤下只对「服务跑着」这件事负责。** 原生内容被撤下之后，`content/posts.mjs` 里其实还在
  （这是故意的）：合成层会把它挡住（410），每一页也都不再列它。
  想真删就改源文件；想恢复就删掉 `data/overrides.json` 里那一条，或者在提示条上点「撤销」。
- 没有搜索、没有 RSS、没有评论。想加就加，别让骨架替你决定。

## 设计资产

<p align="center">
  <img src="design/cv01-keyvisual.png" alt="观测图海报：上方卷帘是九个声部，下方频谱是它们的混合" width="300">
</p>

| 文件 | 是什么 |
|---|---|
| `design/keyvisual.html` | 观测图海报的源文件——独立的作品视图，浏览器直接打开 |
| `design/cv01-keyvisual.png` / `.pdf` | 导出的海报（2400 × 3394），现行配色（r3 中性地面 + 青窗） |
| `design/preview-home.png` | 首页预览（1600 × 1250，亮态，播放头扫完之后的静帧），[根 README](README.md) 顶上那张 |

海报不是插画：它是同一批数据的另一个视图。上方卷帘是九个声部，下方频谱是它们的混合，
两张图共用同一条时间轴；频谱里最深的那些水平条纹就是九个基频（实测与标准音高偏差在 ±5 音分内）。

所有视觉均为原创 CSS 与 SVG，不含任何第三方插画素材。
随站自带的字体 Big Shoulders 以 SIL Open Font License 分发（见 `assets/fonts/BigShoulders-OFL.txt`）。
