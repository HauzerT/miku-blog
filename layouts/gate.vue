<script setup>
/* ==========================================================================
   layouts/gate.vue · 门厅自己那张壳
   ---------------------------------------------------------------------------
   站内页面那套外壳（每日一句 / 命令栏 / 轨道栏 / 页脚 / 悬浮球）这里一样都不摆：
   整屏就是一块青色的音符块，只有一条竖着的尺与两颗键（见 login.css）。
   与旧的 login.html 一一对应——它当年也只加载 palette / tokens / base / login。
   ========================================================================== */
useHead({
  link: [{ rel: 'stylesheet', href: '/assets/css/login.css' }],
  /* 门厅的地是青，所以地址栏也是青：这一页不跟配色轮次走，只跟主题走 */
  meta: [{ name: 'theme-color', content: '#39c5bb' }],
})
</script>

<template>
  <slot />
</template>
